# CPlanner 작업 기록 — 2026-04-29

## 작업 요약

1. 프로젝트 구조 분석 및 파일별 역할 정리
2. `MLModelTestView.swift` 신규 추가 — 종합 ML 모델 테스트 도구
3. `ContentView.swift` 통합 — 헤더 버튼 + 시트 진입점
4. **버그 수정**: `Mistral` 추론 시 발생하던 `Cannot retrieve vector from IRValue format int32` 에러 해결 (causalMask shape)
5. Prefill 최적화 검토 → **미적용 결정** (token-by-token 유지)

---

## 1. 프로젝트 구조 분석

`CPlanner`는 대학생을 위한 AI 플래너로, 로컬 Mistral 7B (CoreML, Int4 양자화)로 할 일을 폴더에 자동 분류한다. macOS 15.0+ / iOS 18.0+. 모든 추론은 온디바이스.

### 파일별 역할

| 파일 | 역할 |
|---|---|
| `CPlannerApp.swift` | 앱 진입점. `init()`에서 `LocalLLMService.preload()` 백그라운드 호출. 단일 `WindowGroup`. |
| `ContentView.swift` | 메인 UI 통합 (455줄). `TaskItem`/`FolderRule` 모델, `TaskManager: ObservableObject` (UserDefaults 영속성 + EventKit 캘린더 + 폴더 감시), `CustomCalendarView` (수제 캘린더), `ContentView`, `SettingsView`. |
| `LocalLLMService.swift` | Mistral 7B 추론 서비스 (`actor`). `preload()` 모델 로딩, `classifyTask()` 폴더 A–Z 매핑 후 Mistral Instruct v3 프롬프트로 토큰별 추론, top-10 logits에서 알파벳 추출. 프롬프트 인젝션 방지(`[INST]`/`</s>` 제거). `validateFileContext()` 무관 파일 필터링. |
| `NLPTestView.swift` | 단순 분류 데모 뷰 (메인 플로우 미연결). |
| `FolderMonitor.swift` | `DispatchSourceFileSystemObject`로 폴더 변경 감시. POSIX `open(O_EVTONLY)` 기반. |
| `Assets.xcassets/` | AppIcon, AccentColor (블루 테마). |
| `StatefulMistral7BInstructInt4.mlpackage` | Mistral 7B Int4 양자화 모델 (~3.8GB). |
| `tokenizer.json` / `tokenizer.model` / `tokenizer_config.json` | HuggingFace + SentencePiece 토크나이저. `AutoTokenizer.from(modelFolder:)`로 로드. |
| `dummy.cpp` / `dummy.hpp` | C++ 인터롭 활성화용 placeholder. |

### 아키텍처
- **UI**: SwiftUI 단일 윈도우, 시트 모달만 사용 (NavigationStack/TabView 없음)
- **상태**: `@StateObject`/`@Published` MVVM-lite, UserDefaults JSON 영속화
- **동시성**: `actor LocalLLMService` + `Task.detached` + `MainActor.run`
- **권한**: 캘린더(EventKit), 폴더 접근(보안 스코프 북마크)
- **Xcode 16 `PBXFileSystemSynchronizedRootGroup`** 사용 — `CPlanner/` 디렉터리 파일은 자동으로 빌드 타겟에 포함됨

---

## 2. ML 모델 테스트 뷰 신규 추가

### 신규 파일: `CPlanner/MLModelTestView.swift`

기존 `NLPTestView`보다 강력한 진단 도구. 다음 기능 제공:

| 섹션 | 기능 |
|---|---|
| 모델 상태 표시 | 워밍업/로드 완료를 색상 점으로 표시. 뷰 진입 시 `.task`로 자동 워밍업. "재워밍업" 버튼 제공. |
| 카테고리 동적 편집 | 칩 형태 추가/삭제 (커스텀 `FlowLayout` 사용). 26개 알파벳 라벨 한계 카운터. 초기화 버튼. |
| 분류 테스트 | 5개 프리셋 한 번 클릭 입력. `Task.detached` + `MainActor.run` 패턴. 추론 시간(`%.2fs`) 측정. 폴백("일반")은 주황색 강조. |
| 파일 검증 테스터 | `validateFileContext` (nonisolated) 직접 호출. ✅/❌ 즉시 표시. |
| 테스트 히스토리 | 최근 20개 누적, 평균 추론 시간 + 폴백 비율 통계. |

**보조 타입**: `ClassificationResult`, `TestRecord`, `FileValidation`, `ModelState` enum.

**`FlowLayout`**: SwiftUI `Layout` 프로토콜 기반 자체 구현. 카테고리 칩 줄바꿈 배치용.

### `ContentView.swift` 통합 (3곳 변경)

```swift
// 311행: 시트 표시 상태 추가
@State private var showMLTest = false

// 327행: 헤더에 🧠(brain.head.profile) 버튼 추가
Button(action: { showMLTest.toggle() }) {
    Image(systemName: "brain.head.profile").font(.title2).foregroundColor(.secondary)
}.buttonStyle(.plain).padding(.trailing, 10).help("ML 모델 테스트")

// 379행: 시트 모디파이어 추가
.sheet(isPresented: $showMLTest) { MLModelTestView() }
```

---

## 4. Prefill 최적화: 미적용 결정

### 검토한 대안
전체 prompt를 한 번에 처리:
- `inputIds: [1, N]` (전체 토큰)
- `causalMask: [1, 1, N, N]` (하삼각, 위쪽은 `-65504.0`)
- 단일 `model.prediction()` 호출 → logits shape `[1, N, vocab]`에서 `[0, N-1, :]` 추출

분류 1회당 **N번 → 1번 호출**로 줄어 N배 빠름.

### Prefill의 단점 (token-by-token 유지 결정 근거)

1. **메모리 O(N²)**: mask가 `N × N` Float16. N=400이면 320KB, N=2048이면 8MB 매 호출 할당. 내부 attention activation도 quadratic.
2. **취소 단위가 거침**: 단일 `prediction()`은 중간 중단 불가. 토큰별은 매 토큰 cancellation 체크 가능.
3. **logits 추출 코드 변경 필요**: `[1, N, vocab]`에서 마지막 위치 슬라이싱.
4. **짧은 prompt에선 이점 미미**: 5~10 토큰이면 오히려 느릴 수 있음 (현재 prompt는 200+ 토큰이라 해당은 안 됨).
5. **첫 호출 ANE 그래프 컴파일 비용 추가**: shape별 캐싱이라 N이 매번 다르면 캐시 미스.
6. **디버깅 가시성 감소**: blackbox 한 번. 토큰별은 중간 logits 관찰 가능.
7. **메인 actor 점유 시간 증가**: 큐에 다른 분류 요청이 쌓이면 더 오래 기다림.
8. **multi-token generation 확장 시 prefill+token-by-token 두 패턴 공존 필요**.

→ 사용자 결정: **token-by-token 유지** (단점 회피 + 안정성 우선).

---

## 변경 파일 목록

```
신규
  CPlanner/MLModelTestView.swift

수정
  CPlanner/ContentView.swift          (3곳: 311, 327, 379행)
  CPlanner/LocalLLMService.swift      (127-134행: causalMask shape 동적화)
```

---

## 알아둘 것

- 현재 prompt 길이는 200~400 토큰 수준. 폴더가 26개 가까이 추가되거나 task title이 매우 길면 2048 한계에 가까워질 수 있음. 안전을 위해 길이 가드를 추가하는 것을 향후 고려.
- `NLPTestView.swift`는 더 이상 필요 없으면 삭제해도 됨 (메인 플로우 미연결).
- Claude Code CLI에서 보였던 `Cannot find 'LocalLLMService' in scope` / `No such module 'Tokenizers'` 진단은 SourceKit이 SPM/Xcode 컨텍스트 없이 분석하는 한계 때문에 생긴 가짜. Xcode에서 빌드/인덱싱하면 해소됨.
